import { GuichetFermerPipe } from './guichet-fermer.pipe';

describe('GuichetFermerPipe', () => {
  it('create an instance', () => {
    const pipe = new GuichetFermerPipe();
    expect(pipe).toBeTruthy();
  });
});
